import { TLocaleOption } from '@/components/Header/LocaleSwitcher/types';

export const ENG_LOCALE: TLocaleOption = {
  icon: '🇺🇸',
  label: 'ENG',
  value: 'en',
};

export const DEFAULT_LOCALE = ENG_LOCALE;

export const LOCALE_OPTIONS: TLocaleOption[] = [ENG_LOCALE];
