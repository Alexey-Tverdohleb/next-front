import { IWalletState } from './types';

const initialState: IWalletState = {
  address: undefined,
  balances: undefined,
  adaHandles: undefined,
  // disableUpdates: {},
  // overrides: {},
};

export default initialState;
