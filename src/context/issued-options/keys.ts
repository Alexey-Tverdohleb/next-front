import { ISSUED_OPTIONS_FILTERS } from '@/constants/filters';

export const KEYS = [
  ISSUED_OPTIONS_FILTERS.startDate,
  ISSUED_OPTIONS_FILTERS.endDate,
  ISSUED_OPTIONS_FILTERS.tokenName,
];

export const DATE_KEYS = [ISSUED_OPTIONS_FILTERS.startDate, ISSUED_OPTIONS_FILTERS.endDate];
