import { OPTIONS_HISTORY_FILTERS } from '@/constants/filters';

export const KEYS = [
  OPTIONS_HISTORY_FILTERS.startDate,
  OPTIONS_HISTORY_FILTERS.endDate,
  OPTIONS_HISTORY_FILTERS.tokenName,
  OPTIONS_HISTORY_FILTERS.optionType,
];

export const DATE_KEYS = [OPTIONS_HISTORY_FILTERS.startDate, OPTIONS_HISTORY_FILTERS.endDate];
