import { IFormValues } from '@/forms/MintingForm/types';

const INITIAL_VALUES: IFormValues = {
  typeOption: 'put',
  strikePrice: '',
  amount: '',
  expireDate: '',
  token: undefined,
};

export default INITIAL_VALUES;
